[canvas_servers]
# Replace with your actual server IP or hostname
canvas-server ansible_host=YOUR_SERVER_IP ansible_user=ubuntu

[canvas_servers:vars]
# SSH key file (update path as needed)
ansible_ssh_private_key_file=~/.ssh/id_rsa
# Uncomment if using password authentication instead
# ansible_ssh_pass=YOUR_SSH_PASSWORD